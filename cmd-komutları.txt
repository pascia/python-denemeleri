cd - direksiyon
cd .. - üst direksiyon
dir - iç bilgi
copy NUL <> - oluştur (null kopyalama)
mkdir - klosör oluşturma
del - del dosya
rmdir - klasör silme
rmdir /s - içi dolu klasör silme

git komutları

git clon <url>
git add <dosya>
git commit -m "mesaj"
git commit -am "mesaj" #all files
git status #bakmak için
git push

git reset --hard <hash>
git reset --hard origin/master

git branch
git checkout -b <isim>
git checkout <branch>
git merge <branch>

